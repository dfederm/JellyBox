<Page
    x:Class="JellyBox.Views.Library"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Behaviors="using:JellyBox.Behaviors"
    xmlns:controls="using:JellyBox.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBase}">

    <Grid XYFocusKeyboardNavigation="Enabled">
        <Grid Height="100" VerticalAlignment="Top" Canvas.ZIndex="1" IsHitTestVisible="False">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FF0A0A0A" Offset="0.0" />
                    <GradientStop Color="#FF0A0A0A" Offset="0.6" />
                    <GradientStop Color="#000A0A0A" Offset="1.0" />
                </LinearGradientBrush>
            </Grid.Background>
            <TextBlock
                Text="{x:Bind ViewModel.Title, Mode=OneWay}"
                FontSize="{StaticResource FontXL}"
                FontWeight="SemiBold"
                Foreground="{StaticResource TextPrimary}"
                VerticalAlignment="Top"
                Margin="48,24,0,0" />
        </Grid>

        <GridView
            Padding="48,100,48,48"
            ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
            ItemTemplate="{StaticResource Card}"
            ItemContainerStyle="{StaticResource CardGridViewItemStyle}"
            SelectionMode="None"
            IsItemClickEnabled="True"
            SingleSelectionFollowsFocus="False"
            TabNavigation="Once"
            IsTabStop="False"
            XYFocusKeyboardNavigation="Enabled"
            ScrollViewer.VerticalScrollBarVisibility="Hidden"
            ScrollViewer.VerticalScrollMode="Enabled"
            ScrollViewer.HorizontalScrollMode="Disabled"
            ScrollViewer.BringIntoViewOnFocusChange="False">
            <Interactivity:Interaction.Behaviors>
                <Behaviors:FocusFirstItemBehavior />
                <Behaviors:ListViewBaseCommandBehavior />
                <Behaviors:ScrollOnFocusBehavior SafeZoneMargin="48" EnableVerticalScroll="True" TrapFocusAtRightEdge="False" TopOffset="100" />
            </Interactivity:Interaction.Behaviors>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>

        <controls:LoadingOverlay Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
    </Grid>
</Page>
